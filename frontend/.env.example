# Frontend environment variables
# Copy to .env for local development (non-Docker)
# For Docker, API calls go through Nginx proxy → no VITE_API_BASE_URL needed

# Local dev only (khi chạy npm run dev, không dùng Docker)
VITE_API_BASE_URL=http://localhost:3000/api
